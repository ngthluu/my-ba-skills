{
  "name": "my-ba-skills-checks",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/version.mjs validate",
    "test:install": "node --test tests/installation.test.mjs"
  },
  "engines": {
    "node": ">=22.20.0"
  },
  "devDependencies": {
    "skills": "1.5.25"
  }
}
